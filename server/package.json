{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build-client": "cd ../client && npm install && npm run build",
    "nipun": "cd ../client && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "fetch": "^1.1.0",
    "mongoose": "^8.18.1"
  }
}
